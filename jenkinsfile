// This is to build maven and identify it's version

pipeline {
    agent any
    tools {
        maven 'MAVEN_PATH'
    }
    stages {
        stage ('Maven stage'){
            steps {
                echo "This is to view maven version"
                sh 'mvn --version'
            }
        }
    }
}
